import random
import math

#   initialization of upper and lower bound
lower_bound = int(input("Enter lowerbound interval: "))
upper_bound = int(input("Enter upperbound interval: "))

#   initialization of count
count = 0

